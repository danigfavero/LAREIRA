/* Programa principal. Note o laco vazio na chamada de yyparse. O corpo do laco
pode conter a chamada para as animaçoes e eventual outro coódigo que precise ser executado
a cada passo (imprimir o status, etc). */
